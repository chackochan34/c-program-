#include<stdio.h>
int add(int n);

int main(){
	int sum;
    printf("enter a value");
    scanf("%d",&sum);
    printf("sum=%d",add(sum));
    return 0;
}

	int add(int n)
	{
	if(n !=1){
		return n * add(n-1);
	}
	else {
		return n;
	}
	
	
}
